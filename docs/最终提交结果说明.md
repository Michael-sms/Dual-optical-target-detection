# 最终提交结果说明

最终方案已冻结，采用四模型结果级融合方案作为提交结果：

- 提交文件：`outputs/result_ens4_nms68_s003_compact.json`
- 融合模型：E1 fixed fusion（v8s，NMS=0.8）+ E1 fixed fusion（v8m，NMS=0.8）+ E2 QAF + E3 QAF+P2
- 融合策略：合并预测结果后按类别执行 NMS，`NMS IoU=0.68`，`score_threshold=0.003`
- 模型大小：`52.91M`
- 最终线上 mAP：`0.278432`

该结果为当前项目最终提交口径。报告中应写作“最终采用四模型结果级融合”，不要写成“最终采用 E2”或“三模型最终融合”。

v8m E1 的单模型线上 mAP 为 `0.244951`，低于 v8s E1 的 `0.246919`，但加入融合后提供了有效互补。原三模型融合线上 mAP 为 `0.271375`，加入 v8m 并调整融合 NMS 后进一步提升：

```text
0.278432 - 0.271375 = 0.007057
```

相比最优单模型 E1（线上 mAP `0.246919`），最终融合结果提升：

```text
0.278432 - 0.246919 = 0.031513
```

即 mAP 提高约 `3.15` 个百分点。最终提交采用该四模型融合结果。
