# 无人机视角下双光目标检测项目计划书

> 项目代号：DualSight-Det  
> 团队规模：4 人  
> 计划版本：v1.1（2026-06-19，十日冲刺版）  
> 项目周期：2026-06-19 至 2026-06-28（6 月 27 日完成正式提交，6 月 28 日仅作验收与应急缓冲）  
> 数据范围：仅使用课程提供的 RGB-T 数据；允许使用并如实报备开源预训练权重

## 1. 项目概述

本项目面向无人机视角下的 RGB（可见光）与 TIR（热红外）成对图像，检测小轿车、卡车、公交车、厢式货车/面包车和大货车五类目标。无人机图像中目标尺度偏小、密集遮挡多，且可见光模态容易受夜间、逆光和天气影响；TIR 在低照度下更稳定，但纹理和类别细节较弱。因此，本项目将以轻量级单阶段检测器为基线，构建“**质量感知的双流跨模态融合 + 小目标增强**”模型，在提高 COCO AP 的同时控制模型参数量。

最终交付包括：测试集检测结果 JSON、可复现实验代码及 README、3 页以内实验报告、成员分工与贡献表，以及预训练模型来源和 MD5 记录。

## 2. 任务要求与验收口径

### 2.1 检测类别

| category_id | 类别名称 | 中文说明 |
|---:|---|---|
| 1 | car | 小轿车 |
| 2 | truck | 卡车 |
| 3 | bus | 公交车 |
| 4 | van | 厢式货车/面包车 |
| 5 | freight_car | 大货车 |

### 2.2 官方评估规则

- 主排名指标为 COCO 风格 **AP@[0.50:0.95]**，同时观察 AP50 和 AP75。
- 若 AP 相同，模型参数大小将用于进一步排名；因此不能只追求大模型精度。
- 测试结果为单个 JSON List，每个预测框格式如下：

```json
{
  "image_id": 1,
  "category_id": 1,
  "bbox": [154.0, 158.0, 25.0, 63.0],
  "score": 0.95
}
```

- `image_id` 为文件名去掉后缀后转为整数，例如 `00001.jpg -> 1`。
- `bbox` 必须为 `[x, y, width, height]`，坐标原点为左上角；推理程序内部若使用 `xyxy`，导出时必须转换。
- 坐标建议保留 1 位小数，置信度必须是合法浮点数；不得输出 NaN、Infinity、负宽高或越界框。
- 网页提交时同时填写模型大小；提交后正常情况下约 30 秒可见结果，超过 5 分钟后台会停止评测。

### 2.3 数据与合规约束

- 禁止未经批准使用外部数据集。
- 可使用开源预训练模型；最终文档必须记录下载地址、许可证、文件名和实际 MD5。
- 任何增强都只作用于课程训练数据；成对图像的几何增强必须使用同一随机参数。

## 3. 数据核查与问题分析

### 3.1 本地数据核查结果

| 划分 | RGB | TIR | 标注图像 | 标注框数 | 配对情况 |
|---|---:|---:|---:|---:|---|
| train | 17,990 | 17,990 | 17,990 | 316,409 | 文件名完全配对 |
| val | 1,469 | 1,469 | 1,469 | 24,490 | 文件名完全配对 |
| test | 1,000 | 1,000 | 无公开标注 | - | 文件名完全配对 |

所有抽检图像分辨率均为 `640 × 512`；TIR 文件以三通道 RGB 形式存储，但视觉内容为灰度热红外。训练和验证标注均为 COCO JSON，类别编号与提交要求一致。

### 3.2 类别分布

| 类别 | 训练框数 | 训练占比 | 验证框数 |
|---|---:|---:|---:|
| car | 270,348 | 85.44% | 20,588 |
| truck | 15,833 | 5.00% | 1,470 |
| bus | 11,334 | 3.58% | 789 |
| van | 7,701 | 2.43% | 725 |
| freight_car | 11,193 | 3.54% | 918 |

按 COCO 面积阈值粗略统计，训练集中小目标（面积小于 `32²`）有 43,951 个，约占 13.9%；中等面积目标占多数。结合无人机俯视、密集车辆与测试域变化，项目的主要困难为：

1. **长尾类别**：car 数量远高于其他四类，容易压制少数类分类梯度。
2. **小目标与密集目标**：下采样导致细节丢失，NMS 还可能误删相邻车辆。
3. **模态质量动态变化**：夜间 RGB 退化，而白天或热背景下 TIR 不一定占优，固定比例融合不合理。
4. **跨模态轻微错位**：成对传感器存在视差或标定误差，直接逐像素相加可能产生重影特征。
5. **测试域偏移**：测试集与训练集采集时间不同，天气、光照和场景分布存在变化。

## 4. 技术路线

### 4.1 基线选择

首选 **YOLOv8s** 作为工程基线：训练和导出流程成熟，速度与模型大小适中，便于增加双流输入和开展消融。若计算资源不足，先用 YOLOv8n 验证流程；最终模型在 YOLOv8n/s 的验证 AP 与模型大小之间选择。

必须建立以下可比较基线：

| 编号 | 输入/方法 | 目的 |
|---|---|---|
| B0 | RGB 单模态 YOLOv8s | 测量可见光上限与低照度短板 |
| B1 | TIR 单模态 YOLOv8s | 测量红外的独立贡献 |
| B2 | RGB+TIR 六通道早期拼接 | 获得最低成本双模态基线 |
| B3 | 双流骨干 + 固定融合 | 排除“仅增加参数量”带来的增益 |
| P | 本项目创新模型 | 与 B3 同训练策略做公平比较 |

### 4.2 创新方案：QAF-SOD 双光检测器

创新模型命名为 **QAF-SOD（Quality-Aware Fusion for Small-Object Detection）**，核心由三个相互配合、可独立消融的设计构成。

#### 创新点一：模态质量感知门控融合（QAF）

RGB 与 TIR 分别使用轻量双流骨干提取多尺度特征。为控制模型大小，两个分支共享骨干的模块结构，并在中高层使用共享卷积权重、独立归一化参数。对第 `l` 层特征 `F_rgb^l`、`F_tir^l`：

1. 分别进行全局平均池化和全局标准差池化，得到亮度、对比度与响应强度的质量描述；
2. 通过轻量 MLP 预测每个尺度的模态权重 `w_rgb^l` 与 `w_tir^l`，经 Softmax 约束两者和为 1；
3. 使用“加权共有信息 + 绝对差异信息”融合：

```text
F_fuse^l = Conv(w_rgb^l * F_rgb^l + w_tir^l * F_tir^l)
           + alpha_l * Conv(|F_rgb^l - F_tir^l|)
```

质量门控让模型根据场景动态选择模态，差异分支保留单一模态特有线索。融合安排在 P3、P4、P5 尺度，避免原图级逐像素融合对轻微错位过度敏感。

#### 创新点二：高分辨率小目标检测头（P2 Head）

在常规 P3-P5 检测头基础上增加步长为 4 的 P2 检测头，将浅层边缘与位置细节送入检测颈部。P2 采用深度可分离卷积并限制通道数，以较小参数代价改善小目标召回。训练与验证额外报告小目标 AP、按目标面积分桶的 Recall，判断增益是否确实来自小目标。

#### 创新点三：模态退化增强与一致性约束（MDC）

训练时以小概率对单个模态做退化，模拟真实采集变化：RGB 使用亮度、对比度、色偏、雾化或局部遮挡；TIR 使用对比度压缩、噪声和局部遮挡。任何裁剪、翻转、缩放等几何变换在两模态间严格同步。

对正常双模态输入和随机弱化某一模态的输入，约束其融合特征或分类分布保持一致：

```text
L_total = L_det + lambda_cons * L_cons
```

其中 `L_det` 为检测损失，`L_cons` 仅在匹配位置计算，并采用较小权重和延迟启用策略，避免训练早期错误约束。该设计不增加推理参数，可提升对夜间 RGB 失效或 TIR 热对比不足的鲁棒性。

### 4.3 类别不均衡与密集目标策略

- 分类损失优先比较原始 BCE 与 Focal Loss；若框架使用动态标签分配，则只改变一个因素并做消融。
- 数据采样以“包含少数类的图像加权采样”为候选，禁止简单复制单框或破坏时序/场景分布。
- 增强配置保守使用 Mosaic、MixUp 和随机尺度；双模态几何操作完全同步，并在训练末段关闭强增强。
- 密集场景比较标准 NMS、Soft-NMS 或适度提高 NMS IoU；阈值只在验证集调优，不依据测试榜单反复过拟合。
- 每类分别检查 AP 和混淆矩阵，尤其关注 truck、bus、van、freight_car 之间的混淆。

## 5. 数据与代码实现方案

### 5.1 推荐目录结构

```text
Dual-optical target detection/
├── data/                       # 课程数据，不提交到代码仓库
│   ├── train/{rgb,tir,train.json}
│   ├── val/{rgb,tir,val.json}
│   └── test/{rgb,tir}
├── configs/                    # 数据、模型、训练、推理配置
├── dualdet/
│   ├── datasets/               # RGB-T 成对加载与同步增强
│   ├── models/                 # 双流骨干、QAF、P2 Head
│   ├── losses/                 # 检测与一致性损失
│   └── utils/                  # 评估、日志、JSON 校验
├── scripts/                    # train/eval/infer/export/validate_json
├── tests/                      # 配对、坐标转换、JSON 格式单测
├── outputs/                    # 权重、日志、预测，不纳入版本控制
├── docs/
├── README.md
└── requirements.txt
```

### 5.2 数据管线验收项

- 按 COCO `images.id` 建立样本索引，不依赖目录枚举顺序。
- 每次读取前断言 RGB、TIR 同名且尺寸一致；缺失配对直接报错，不静默跳过。
- 对 TIR 明确采用三通道读取或单通道后复制，训练和推理保持一致。
- 同步增强后，检查框坐标合法并过滤过小/无效框。
- 用 16 个样本画出 RGB、TIR 与框的可视化网格，由两名成员交叉检查。
- 固定随机种子，保存完整配置、代码提交号、硬件、训练时长和最佳 epoch。

### 5.3 推荐初始训练配置

以下为起始值，最终以显存和验证集结果为准：

| 项目 | 初始设置 |
|---|---|
| 输入尺寸 | 640×512；可比较 800×640 的收益与显存代价 |
| 优化器 | AdamW 或框架默认优化器，统一基线设置 |
| 训练轮数 | 80-120 epochs，早停 patience 15-20 |
| 学习率 | 按有效 batch size 线性缩放，使用 warmup + cosine decay |
| 精度 | AMP 混合精度 |
| 模型选择 | 以验证 AP@[.50:.95] 最佳 checkpoint 为准 |
| 日志 | 总损失、各子损失、AP/AP50/AP75、每类 AP、参数量、FLOPs、显存与推理时延 |

为保证实验公平，所有消融实验使用相同数据划分、随机种子、输入尺寸、预训练权重和训练轮数。十日周期内，筛选实验采用单随机种子；最终固定双流基线与完整模型原则上各补 2 个随机种子并报告均值和波动。若算力不足，则优先保证两者使用同一种子公平复验，并在报告中如实说明限制，不以未完成的重复实验阻塞正式提交。

## 6. 实验矩阵与决策规则

### 6.1 核心消融

| 实验 | 固定双流 | QAF | 差异分支 | P2 | MDC | 目的 |
|---|:---:|:---:|:---:|:---:|:---:|---|
| E0 | - | - | - | - | - | RGB 基线 |
| E1 | ✓ | - | - | - | - | 双流参数增益基线 |
| E2 | ✓ | ✓ | - | - | - | 门控贡献 |
| E3 | ✓ | ✓ | ✓ | - | - | 跨模态差异贡献 |
| E4 | ✓ | ✓ | ✓ | ✓ | - | 小目标头贡献 |
| E5 | ✓ | ✓ | ✓ | ✓ | ✓ | 完整模型与鲁棒性 |

每个实验至少记录：总体 AP、AP50、AP75、五类 AP、小目标 AP/Recall、参数量、权重文件大小、FLOPs、单张推理时延。若模块带来的 AP 增益小于随机波动，或单位模型大小收益明显不合理，则不进入最终模型。

### 6.2 鲁棒性测试

在验证集构造不参与训练的退化版本，分别测试：低亮度 RGB、模糊 RGB、低对比度 TIR、RGB 缺失、TIR 缺失以及轻微像素平移。退化集只用于分析，不替代官方验证集。重点比较 B3 与 E5 的 AP 降幅，证明质量感知融合并非仅对常规场景有效。

### 6.3 最终模型选择

采用双目标决策：

1. 首先选择验证 AP 较高且多次运行稳定的模型；
2. 若候选模型 AP 差距小于 0.3 个百分点，优先选择参数量和权重文件更小者；
3. 阈值、NMS 和 TTA 必须在验证集确定；TTA 仅在 AP 收益足以抵消推理成本时启用；
4. 测试网页用于最终确认，不进行无记录的高频试探。

## 7. 四人分工与协作机制

姓名尚未提供，以下以角色编号表示；组队后只需替换姓名，不改变责任边界。每位成员都有独立可验收产出，最终贡献度依据提交记录、实验记录与交付质量评定。

| 成员 | 角色与主责 | 具体任务 | 可验收产出 | 计划贡献 |
|---|---|---|---|---:|
| 成员 1（负责人） | 项目统筹、主模型与集成 | 制定规范；实现双流骨干、QAF 与差异分支；合并代码；控制变量；选择最终模型；组织提交 | 模型核心代码、主配置、周报/决策记录、最终 checkpoint | 30% |
| 成员 2 | 数据与训练工程 | COCO 数据核查；成对 Dataset；同步增强；类别/尺度统计；基线 B0-B3；训练脚本和复现环境 | 数据检查报告、Dataset/增强代码、基线权重与日志、环境清单 | 25% |
| 成员 3 | 小目标、损失与实验分析 | 实现 P2 Head、MDC 与一致性损失；类别不均衡实验；完成 E2-E5 消融和鲁棒性测试 | 模块代码、消融表、每类 AP/混淆矩阵、鲁棒性分析 | 25% |
| 成员 4 | 评测、推理与文档 | 实现 COCO 评估、推理导出与 JSON 校验；统计参数量/速度；网页提交；维护 README；主笔 3 页实验报告与贡献表 | `result.json`、校验脚本、提交记录、README、报告与贡献表 | 20% |

### 7.1 共同责任

- 每个模块至少由一名非作者成员完成代码评审。
- 训练结果不得只在聊天中汇报，必须登记到统一实验表并附配置和日志路径。
- 成员 1 与成员 4 共同执行最终提交；成员 2 复核数据/类别映射；成员 3 复核模型权重与推理结果。
- 贡献比例为计划值，最终按有效提交、实验完整性和问题解决记录据实调整。

## 8. 进度安排

项目必须在 2026 年 6 月 28 日前结束。以 6 月 19 日为第 1 天，共安排 10 个自然日；**6 月 24 日晚冻结模型结构，6 月 26 日晚冻结代码，6 月 27 日完成正式提交，6 月 28 日不再开展新实验**。这样即使网页评测或材料打包出现问题，仍保留一天应急时间。

| 天数 | 日期 | 四人并行工作 | 当日必须产出/退出条件 |
|---:|---|---|---|
| Day 1 | 6 月 19 日 | 成员 1 固化技术方案与仓库规范；成员 2 完成数据配对、类别和框统计；成员 3 准备模型配置与实验表；成员 4 搭建 COCO 评估、JSON 校验和 README 骨架 | 数据检查通过；训练、验证、推理命令骨架可运行；任务看板建立 |
| Day 2 | 6 月 20 日 | 成员 2 训练 RGB 基线 B0；成员 3 准备 TIR 基线 B1；成员 1 验证 YOLOv8n/s 的显存与速度；成员 4 完成本地评估及可视化 | B0 至少完成一次有效训练；评估指标与可视化正确；确定后续主干规模 |
| Day 3 | 6 月 21 日 | 完成 B1 与六通道早期融合 B2；同步编写成对加载器和同步增强单测；成员 4 用假预测完成测试 JSON 全流程演练 | B0/B1/B2 结果可比较；RGB-T 同步增强通过；JSON 校验器通过 |
| Day 4 | 6 月 22 日 | 成员 1、2 实现并训练固定双流基线 B3；成员 1、3 实现 QAF 门控；成员 4 开始整理报告的数据与方法章节 | B3 可复现；QAF 前向传播、损失反传和参数量统计通过 |
| Day 5 | 6 月 23 日 | 训练 E2（B3+QAF）；并行实现差异分支和轻量 P2 Head；检查少数类 AP、显存和推理速度 | 获得 E2 完整验证结果；差异分支/P2 可由配置独立开关 |
| Day 6 | 6 月 24 日 | 完成 E3、E4 的时间盒消融；若资源允许再测试 MDC；比较 AP、每类 AP、小目标指标和模型大小 | **22:00 前冻结模型结构**；选定最终模型和一个备用模型；未证明有效的模块立即删除 |
| Day 7 | 6 月 25 日 | 用冻结配置训练最终候选；固定双流基线和完整模型做公平复验；成员 4 同步完善 3 页报告和 README | 生成最终候选 checkpoint；主要实验表完成；预训练来源与 MD5 登记完成 |
| Day 8 | 6 月 26 日 | 完成验证、误差分析、参数量/时延统计；对 1,000 对测试图推理；双人检查随机样本和 `result.json` | **22:00 前冻结代码与配置**；最终 JSON、备用 JSON、权重和复现命令齐全 |
| Day 9 | 6 月 27 日 | 成员 1、4 在中午前完成网页正式提交；全员交叉检查 README、3 页报告、贡献表和代码包 | 网页出现有效评分；最终材料打包并异地备份；问题在当天完成修正提交 |
| Day 10 | 6 月 28 日 | 仅处理网页故障、格式错误或材料缺失；执行最终验收和归档，不再修改模型或无依据调参 | 截止前确认成绩、提交记录和全部文件可访问；负责人宣布项目结束 |

### 8.1 十日周期的实验优先级

为避免十天内实验面铺得过宽，所有任务按以下优先级执行：

- **P0 必须完成**：RGB 基线 B0、固定双流基线 B3、QAF 创新 E2、测试 JSON 校验、最终提交、README 和 3 页报告。
- **P1 优先完成**：差异分支 E3、轻量 P2 Head E4、最终模型公平复验、每类 AP 与小目标分析。
- **P2 有余力再做**：MDC 一致性训练、鲁棒性退化集、Soft-NMS、输入尺寸比较和 TTA。
- 任何单项调参最多占用半天；连续两次无改善即停止。6 月 24 日结构冻结后，只允许修复错误、调整推理阈值和复验，不再加入新模块。

### 8.2 每日协作节奏

- 每日 10 分钟站会：昨天结果、今天目标、阻塞项。
- 每次实验启动前登记“假设、唯一变量、配置、预期”；完成后登记结果与结论。
- 每日 21:30 统一合并代码与更新实验表，主分支始终保持可运行。
- 关键失败（NaN、指标突降、数据错配）当天建立问题记录并指定负责人。
- 每日 22:00 由负责人检查当日退出条件；未完成项先压缩 P2 工作，不得挤占 6 月 27 日正式提交时间。

## 9. 风险与应对

| 风险 | 预警信号 | 应对措施 | 责任人 |
|---|---|---|---|
| 显存/训练时间不足 | OOM、单实验超预算 | 先 YOLOv8n/子集调通；AMP、梯度累积；减少无依据网格搜索 | 成员 1、2 |
| 双模态错配或增强不同步 | 可视化框/目标不重合 | 文件名断言、共享几何变换参数、批次可视化单测 | 成员 2 |
| 创新模块无稳定增益 | 不同种子结论相反 | 独立消融；检查参数量公平性；无效模块及时删除 | 成员 1、3 |
| 少数类 AP 过低 | 混淆矩阵偏向 car | Focal/加权采样二选一逐项验证；增加少数类错误分析 | 成员 3 |
| JSON 提交失败 | 评测超时或格式报错 | Schema、范围、图像 ID、类别 ID 和有限数校验；先小样例测试 | 成员 4 |
| 过拟合排行榜 | 本地 AP 不升但线上波动 | 冻结验证集决策；限制提交次数；保留每次提交配置 | 成员 1、4 |
| 文档或开源报备缺失 | 无法追溯权重来源 | 下载时立即记录 URL、许可证和 MD5；交付清单逐项签字 | 全员 |

## 10. 结果导出与提交前检查

### 10.1 JSON 自动校验

推理脚本生成 `result.json` 后必须自动检查：

- 根对象是 List，每个元素仅含所需字段；
- `image_id` 属于 1-1000 的测试图像 ID 集合，不把前导零字符串直接写入；
- `category_id` 只能为 1-5；
- `bbox` 长度为 4，`x,y >= 0`，`width,height > 0`，并裁剪到 640×512 范围；
- `score` 在 `[0,1]` 内，所有数字均为有限值；
- 无明显重复框；结果文件可被标准 JSON 解析器重新读取；
- 从 20 张随机测试图生成可视化，人工检查 RGB 和 TIR 场景中的框与类别。

### 10.2 最终交付清单

- [ ] 测试集 `result.json`，网页提交成功并保存得分截图/记录
- [ ] 最终模型权重、模型参数量和权重文件大小
- [ ] 可复现实验代码、固定依赖文件、训练/评估/推理命令
- [ ] README：环境、目录、数据准备、运行方式、实验结果、常见问题
- [ ] 3 页以内实验报告：方法创新、实验设置、消融、结果分析与可靠结论
- [ ] 四人分工与最终贡献度表
- [ ] 所有开源代码/预训练权重的链接、许可证、文件 MD5
- [ ] 最终配置、随机种子、代码版本号和完整日志归档

## 11. 项目成功标准

1. **正确性**：本地 COCO 评估和网页评测均能正常运行，提交格式零错误。
2. **有效性**：完整模型相对参数量可比的固定双流基线获得可复验的 AP 提升，并通过消融说明提升来源。
3. **创新性**：质量感知融合针对模态动态退化，小目标头针对无人机目标尺度，一致性训练提升模态缺失/退化鲁棒性，三者均有定量证据。
4. **轻量性**：同时报告 AP、参数量、模型文件大小和时延；当精度接近时选择更小模型。
5. **可复现性**：新环境按 README 能完成数据检查、训练、验证、推理和 JSON 导出。
6. **协作性**：四名成员职责和产出可追踪，代码至少一人交叉评审，贡献度有客观依据。

---

本计划依据课程提供的《无人机视角下的双光目标检测》和《评审网页的使用》两份材料，以及本地数据目录的实际核查结果制定。执行时若网页规则或截止时间更新，以课程最新通知为准，并由负责人当天更新本文件的版本记录。
